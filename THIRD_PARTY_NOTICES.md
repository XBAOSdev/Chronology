# 第三方依赖与许可声明（THIRD PARTY NOTICES）

本文件记录「大事年表」项目实际使用的第三方资源、许可与出处，供知识产权合规核查。

## 一、结论：零运行时第三方依赖

本项目在**运行时不依赖任何第三方库、框架、CDN、在线字体或在线图标**。

- 页面所有脚本均以本地 `<script src="...">` 顺序加载，全部位于本仓库 `data/` 与 `src/` 目录内。
- 无任何 `https://` / `http://` 外部请求（已在自动化测试中校验：全部网络请求仅命中 `file:`、`data:`、`blob:` 协议）。
- 字体使用系统字体回退栈（思源黑体 → 鸿蒙/小米/OPPO/vivo → 苹方/微软雅黑 → 默认无衬线），**不下载、不内嵌任何字体文件**。
- 图标为项目内手写的内联 SVG 路径，不含第三方图标库字形。
- 页面图标（favicon）为内联 `data:image/svg+xml`，无外部图片。

> 因此，本项目可直接以 `file://` 双击打开运行，也可原样上传至任意静态托管（如 Cloudflare Pages、GitHub Pages、对象存储）。

## 二、自实现的关键算法（非第三方代码）

以下功能均由本项目从零实现，未复制任何第三方库源码：

| 模块 | 文件 | 说明 |
| --- | --- | --- |
| CRC32 校验 | `src/utils/crc32.js` | 使用标准多项式 `0xEDB88320` 的查表法实现（该多项式与算法本身为公开通用算法，非受版权保护的实现） |
| ZIP 打包 / 解包 | `src/utils/zip.js` | 自实现 store（不压缩）模式，含本地文件头、中央目录、EOCD 记录；仅使用公开的 ZIP 文件格式规范 |
| 模糊检索 | `src/utils/fuzzy.js` | 连续子串优先 + 子序列间隔加权的评分算法，自行设计实现 |
| 时间坐标模型 | `src/core/time-model.js` | 「公元 1 年 = x 0」坐标映射与多精度解析，自行设计实现 |
| Canvas 渲染与交互 | `src/render/*`、`src/interaction/*` | 基于浏览器原生 Canvas 2D 与 Pointer Events 从零实现 |

## 三、参考的标准 / 规范（非代码依赖）

- **ZIP 文件格式**（APPNOTE.TXT）：用于 `zip.js` 实现，属公开格式规范。
- **RFC 4180 / JSON（RFC 8259）**：数据导入导出涉及的文本格式规范。
- **Pointer Events / Canvas 2D / CSS 自定义属性**：均由浏览器提供，无需额外依赖。

## 四、开发期工具（不随产物分发）

以下工具仅在**开发与测试阶段**使用，不进入最终交付物，不会产生运行时依赖：

- **Node.js**：用于运行纯逻辑自检脚本与语法检查。
- **Playwright（`playwright-core`）**：仅用于开发期的浏览器端自动化回归测试。

> 上述工具未被打包进 `index.html` 及其引用的任何文件，最终产物中不包含其源码或运行时。

## 五、内容数据来源

内置的「中国史」「世界史」试点时间轴内容为**公开历史史实**的整理归纳（年代、事件简述），事件描述均控制在 150 字以内，由项目作者撰写，不含受版权保护的原文摘录。若后续引入任何第三方数据源，必须在此处补充其名称、许可与出处。

---

_最后更新：2026-09-10_
